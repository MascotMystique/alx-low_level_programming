#include <stdio.h>

/**
 * main - Entry point
 * puts - a function that prints out a string of characters
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
